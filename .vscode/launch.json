{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open Demo in Chrome",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:4000/",
      "webRoot": "${workspaceFolder}/Candy/project",
      "preLaunchTask": "Run Server"
    }
  ]
}
